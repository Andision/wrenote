// Everything components need: the message machinery and the hooks.
// `<I18nProvider>` is imported from "./provider" (only main.tsx needs it), so
// this barrel stays free of component exports.
export * from "./messages";
export * from "./context";
