{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Both windows load the web client from the engine over loopback; they need IPC for the overlay commands, window dragging, and opening the update download in the system browser.",
  "windows": [
    "main",
    "overlay"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*",
      "http://localhost:*"
    ]
  },
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-size",
    "core:window:allow-set-position",
    "opener:default"
  ]
}
