# ---- macOS ----
.DS_Store

# ---- Python ----
__pycache__/
*.py[cod]
*.egg
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# ---- Node / frontend ----
node_modules/
dist/
dist-ssr/
*.local

# ---- Desktop shells ----
# electron-builder output (the packaged .app / installer); regenerated.
shells/electron/release/
# Tauri: cargo build dir + generated schemas (tauri-build writes gen/ on every build)
shells/tauri/src-tauri/target/
shells/tauri/src-tauri/gen/

# Built SPA emitted by `npm run build` (vite outDir); regenerated, not committed.
engine/static/app/

# Packaging build artifacts (regenerated by the build / CI)
packaging/.build_cache/
packaging/build/
packaging/macos/syscap
packaging/macos/screencap
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- Editors ----
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# ---- Models & runtime data (live in ~/.wrenote, never in the repo) ----
*.gguf
*.bin
*.wav
recordings/
models/

# ---- Sample media (large; not redistributed) ----
miscs/

# ---- Local-only stash: docs, samples, scripts not (yet) published ----
.local/
