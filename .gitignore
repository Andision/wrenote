# ---- macOS ----
.DS_Store

# ---- Python ----
__pycache__/
*.py[cod]
*.egg
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# ---- Node / frontend ----
node_modules/
dist/
dist-ssr/
*.local

# ---- Electron desktop shell ----
# electron-builder output (the packaged .app / installer); regenerated.
electron/release/

# Built SPA emitted by `npm run build` (vite outDir); regenerated, not committed.
backend/static/app/

# Packaging build artifacts (regenerated by the build / CI)
backend/packaging/.build_cache/
backend/packaging/macos/syscap
backend/packaging/macos/screencap
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- Editors ----
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# ---- Models & runtime data (live in ~/.wrenote, never in the repo) ----
*.gguf
*.bin
*.wav
recordings/
models/

# ---- Sample media (large; not redistributed) ----
miscs/

# ---- Local-only stash: docs, samples, scripts not (yet) published ----
.local/
